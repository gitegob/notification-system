events {}
http {
  upstream apilimiters {
    server apiratelimiter1:3000;
    server apiratelimiter2:3000;
  }

  server {
    listen 3000;

    location / {
      proxy_pass http://apilimiters;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}
